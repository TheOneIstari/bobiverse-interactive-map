{
  "name": "bobiverse-interactive-map",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate": "node scripts/validate-dataset.mjs",
    "build": "npm run validate && tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.166.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/three": "^0.166.0",
    "playwright": "^1.60.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
